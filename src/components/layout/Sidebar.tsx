"use client"; import React from 'react'; import { Link, useLocation } from 'react-router-dom'; import { LayoutDashboard, Users, Film, FileText, Receipt, MessageSquare, Settings, ChevronLeft, ChevronRight } from 'lucide-react'; import { cn } from '@/lib/utils'; import { motion } from 'framer-motion'; interface NavItemProps { icon: React.ReactNode; label: string; to: string; collapsed: boolean; } const NavItem = ({ icon, label, to, collapsed }: NavItemProps) => { const location = useLocation(); const isActive = location.pathname === to; return ( <Link to={to} className={cn( "flex items-center gap-3 px-3 py-2 rounded-lg transition-all duration-200 group", isActive ? "bg-[#2DD4BF]/10 text-[#2DD4BF]" : "text-gray-400 hover:text-white hover:bg-white/5" )} > <span className={cn("transition-colors", isActive ? "text-[#2DD4BF]" : "group-hover:text-white")}> {icon} </span> {!collapsed && ( <motion.span initial={{ opacity: 0, x: -10 }} animate={{ opacity: 1, x: 0 }} className="text-sm font-medium whitespace-nowrap" > {label} </motion.span> )} </Link> ); }; const Sidebar = ({ collapsed, setCollapsed }: { collapsed: boolean, setCollapsed: (v: boolean) => void }) => { return ( <motion.aside animate={{ width: collapsed ? 80 : 260 }} className="h-screen sticky top-0 bg-[#0B0C0E] border-r border-white/5 flex flex-col relative" > <div className="p-4 flex items-center justify-between"> {!collapsed && ( <span className="text-[#2DD4BF] font-bold text-xl tracking-tight"> VISION FOLD<span className="text-white"> CREATIVE</span> </span> )} <button onClick={() => setCollapsed(!collapsed)} className="p-1.5 rounded-md bg-white/5 text-gray-400 hover:text-white transition-colors" > {collapsed ? <ChevronRight size={18} /> : <ChevronLeft size={18} />} </button> </div> <nav className="flex-1 px-3 space-y-2 mt-4"> <NavItem icon={<LayoutDashboard size={20} />} label="Dashboard" to="/" collapsed={collapsed} /> <NavItem icon={<Users size={20} />} label="Clients" to="/clients" collapsed={collapsed} /> <NavItem icon={<Film size={20} />} label="Projects" to="/projects" collapsed={collapsed} /> <NavItem icon={<FileText size={20} />} label="Invoices" to="/invoices" collapsed={collapsed} /> <NavItem icon={<Receipt size={20} />} label="Expenses" to="/expenses" collapsed={collapsed} /> <NavItem icon={<MessageSquare size={20} />} label="AI Assistant" to="/ai" collapsed={collapsed} /> </nav> <div className="p-3 border-t border-white/5"> <NavItem icon={<Settings size={20} />} label="Settings" to="/settings" collapsed={collapsed} /> </div> </motion.aside> ); }; export default Sidebar;