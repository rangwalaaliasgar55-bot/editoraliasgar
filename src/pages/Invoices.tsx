"use client";

import React from "react";
import ComingSoon from "@/components/ComingSoon";

const Invoices = () => (
  <ComingSoon
    title="Invoices"
    description="Track amounts, due dates, and payment status per client."
  />
);

export default Invoices;
